export type NavItem = {
  label: string;
  href: string;
};

export type ServiceCard = {
  id: string;
  title: string;
  subtitle: string;
  bgColor: string;
  textColor: string;
  subtitleColor: string;
  icon: string;
};

export type AppFeature = {
  id: string;
  title: string;
  description: string;
  icon: string;
};
